# frozen_string_literal: true

source 'https://rubygems.org'

# gem "rails"
group :development do
  gem 'activesupport'
  gem 'debug'
  gem 'ostruct'         # ← Ruby 3.5.0以降の警告対策
  gem 'rake'            # ← 同上
  gem 'rubocop'
  gem 'ruby-lsp'
  gem 'ruby_parser'
  gem 'sorbet'
  gem 'sorbet-runtime'
  gem 'tapioca', require: false
  gem 'thor'            # ← Bundler::Thor::RakeCompat 対策（必要なら）
end
